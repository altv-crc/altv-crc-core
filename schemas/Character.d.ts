declare module 'alt-crc' {
    interface Character {
        _id?: string;

        // Do not append data here.

        // Each plugin that makes modifications to the interface
        // will include their changes under the 'schemas' folder.
    }
}
